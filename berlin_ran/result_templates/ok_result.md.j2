# {{ tn_id }}-{{ entity_name }}

The component `{{ tn_id }}-{{ entity_name }}` has been succesfully created.
The RAN Equipment at `{{ site_available_components.berlin_ran.subnet|default('') }}` is now linked to the 5G Core of component `{{ any_berlin_ran_linked_5gcore }}`.

## Important information:

Routing configuration between your Trial Network and the Berlin RAN will only be active from `{{ current_timestamp }}` to `{{ ending_timestamp }}`.

