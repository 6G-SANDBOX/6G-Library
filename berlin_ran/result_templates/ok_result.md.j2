# {{ tn_id }}-{{ entity_name }}

The component `{{ tn_id }}-{{ entity_name }}` has been succesfully created.
The RAN Equipment at `{{ site_available_components.berlin_ran.cp_ip|default('') }}` is now linked to the 5G Core of component `{{ one_berlin_ran_linked_open5g }}`.

## Important information:

Routing configuration between your Trial Network and the Berlin RAN will only be active from `{{ start_time }}` to `{{ stop_time }}`.

