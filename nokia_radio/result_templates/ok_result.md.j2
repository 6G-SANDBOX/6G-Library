# {{ tn_id }}-{{ entity_name }}

The component `{{ tn_id }}-{{ entity_name }}` has been succesfully created.
The Nokia Airscale at `{{ site_available_components.nokia_radio.cp_ip }}` is now linked to the 5G Core of component `{{ one_nokia_radio_open5gs }}`.

## Important information:

Routing configuration between your Trial Network and the NOKIA RADIO will only be active from `{{ start_time }}` to `{{ stop_time }}`.
