# {{ tn_id }}-{{ entity_name }}

The component `{{ tn_id }}-{{ entity_name }}` has been succesfully created.
Your previously deployed 5G core component (`{{ any_nokia_radio_linked_5gcore }}`) is now linked to the Nokia Airscale at UMA.

The following connections have been enabled:
- Control Plane (N2 interface):
    * **`{{ entity_name }}`**: `{{ site_available_components.nokia_radio.n2_ip }}`
    * **`{{ any_nokia_radio_linked_5gcore }}` AMF**: `{{ core_metadata['amf_n2_ip'] }}`
- User Plane (N3 interface):
    * **`{{ entity_name }}`**: `{{ site_available_components.nokia_radio.n3_ip }}`
    * **`{{ any_nokia_radio_linked_5gcore }}` UPF**: `{{ core_metadata['upf_n3_ip'] }}`

## Important information:

Routing configuration between your Trial Network and the NOKIA RADIO will only be active from `{{ current_timestamp }}` to `{{ ending_timestamp }}`.
