# {{ tn_id }}-{{ entity_name}}

The component `{{ tn_id }}-{{ entity_name }}` has been succesfully created.

It consists of an RICTest Agent Virtual Machine 

## Important information:

- **OpenNebula VM ID**: `{{ id }}`
- **VM memory**: `{{ one_rictest_agent_memory}} MiB`
- **VM VCPUs**: `{{ one_rictest_agent_cpu }}`
- **VM internal storage**: `{{ one_rictest_agent_disk}} MiB`
- **VM IP addresses**:
{% for ip_list in ips.values() %}
{% for ip in ip_list %}
  - `{{ ip }}`
{% endfor %}
{% endfor %}