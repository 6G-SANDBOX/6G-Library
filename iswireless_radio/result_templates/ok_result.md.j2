# {{ tn_id }}-{{ entity_name }}

{# # TODO: Using jinja conditionals, change the result information whether we are in normal or RU-DU mode #}
The component `{{ tn_id }}-{{ entity_name }}` has been succesfully created.
The ISWIRELESS at `{{ site_available_components.iswireless_radio.cu_ip }}` is now linked to the 5G Core of component `{{ any_iswireless_radio_linked_5gcore }}`.

## Important information:

Routing configuration between your Trial Network and the ISWIRELESS RADIO will only be active from `{{ start_time }}` to `{{ stop_time }}`.
