# {{ tn_id }}-{{ entity_name }}

The component `{{ tn_id }}-{{ entity_name }}` has been succesfully created.
Your previously deployed 5G core component (`{{ any_custom_gnb_linked_5gcore }}`) is now linked to your experimenter-provided gNB.

The following connections have been enabled:
- Control Plane (N2 interface):
    {# * **`{{ entity_name }}`**: `{{ site_available_components.custom_gnb.n2_ip }}`    # TODO: change #}
    * **`{{ any_custom_gnb_linked_5gcore }}` AMF**: `{{ core_metadata['amf_n2_ip'] }}`
- User Plane (N3 interface):
    {# * **`{{ entity_name }}`**: `{{ site_available_components.custom_gnb.n3_ip }}` # TODO: change #}
    * **`{{ any_custom_gnb_linked_5gcore }}` UPF**: `{{ core_metadata['upf_n3_ip'] }}`

## Important information:

Routing configuration between the Trial Network and your gNB will only be active from `{{ current_timestamp }}` to `{{ ending_timestamp }}`.
