# {{ tn_id }}-{{ entity_name}}

[Open5Gcore](https://www.open5gcore.org/), an 5G SA core network implementation, has been successfully deployed as a VM as part of the Trial Network **{{ tn_id }}**.

<img src="https://cdn0.scrvt.com/fokus/d9859d6f8b785685/4d3f99755ad3/v/96292ff212e8/NGNI_logo_open5gcore_02.jpg" />

Below is a summary of the components that have been deployed:

* NRF - NF Repository Function
* AMF - Access and Mobility Management Function
* SMF - Session Management Function
* UPF - User Plane Function
* AUSF - Authentication Server Function
* UDM - Unified Data Management
* UDR - Unified Data Repository
* PCF - Policy and Charging Function

Current versions:
- Open5GCore `v9.0.x`
- Ubuntu 22.04

## Important information

- **OpenNebula VM ID**: `{{ id }}`
- **VM memory**: `{{ one_open5gcore_vm_memory}} MiB`
- **VM VCPUs**: `{{ one_open5gcore_vm_cpu }}`
- **VM available storage**: `6 GiB`
- **VM network interfaces**:
  ```yaml
  {{ ips }}
  ```
- **PLMN ID (MCC)**: `{{ one_open5gcore_vm_mcc }}`
- **PLMN ID (MNC)**: `{{ one_open5gcore_vm_mnc }}`
- **TAC**: `{{ one_open5gcore_vm_tac }}`
- **S-NSSAI (SST)**: `{{ one_open5gcore_vm_s_nssai_sst }}`
- **S-NSSAI (SD)**: `{{ one_open5gcore_vm_s_nssai_sd }}`
- **AMF IP**: `{{ open5gs_metadata_dict['amf_ip'] }}`
- **UPF IP**: `{{ open5gs_metadata_dict['upf_ip'] }}`
- **Web CLI URL**: `{{ entity_name | default("UNDEFINED") }}.{{ tn_id }}.{{ site_domain }}`
  - AMF:  [port 13020](http://{{ entity_name | default("UNDEFINED") }}.{{ tn_id }}.{{ site_domain }}:13020/cli)
  - AUSF: [port 13025](http://{{ entity_name | default("UNDEFINED") }}.{{ tn_id }}.{{ site_domain }}:13025/cli)
  - NRF:  [port 13035](http://{{ entity_name | default("UNDEFINED") }}.{{ tn_id }}.{{ site_domain }}:13035/cli)
  - SMF:  [port 13040](http://{{ entity_name | default("UNDEFINED") }}.{{ tn_id }}.{{ site_domain }}:13040/cli)
  - UDM:  [port 13030](http://{{ entity_name | default("UNDEFINED") }}.{{ tn_id }}.{{ site_domain }}:13030/cli)
  - UPF:  [port 13045](http://{{ entity_name | default("UNDEFINED") }}.{{ tn_id }}.{{ site_domain }}:13045/cli)


**Note**: To access the dashboard, you must add the domain to the /etc/hosts file of your machine mapping it with the IP address of your VM Machine.