######################################################
##
## Component public variables
##
## Diverse information read by the TNLCM
## to adapt its behaviour depending on the component.
##
######################################################


####################################
## Component metadata
####################################
metadata:
  maintainers:
    - Carlos Andreo López <c.andreo@uma.es>
  short_description: Deploy a virtual machine with InfluxDB v2.7.11
  long_description: |
    Ubuntu 22.04 LTS virtual machine with InfluxDB v2.7.11 installed. 
    
    InfluxDB is a time-series database designed to handle large volumes of event and metric data. It is ideal for applications that require high performance in data writes, such as infrastructure monitoring, log analysis, or IoT sensor metrics. In this project, InfluxDB is used to store and query time-based data, enabling trend analysis, alerting, and real-time visualization. Its ability to efficiently manage large amounts of data and integration with various visualization tools like Grafana make InfluxDB an excellent solution for such applications.
  hypervisors: ["one"]
  appliances: ["https://marketplace.mobilesandbox.cloud:9443/appliance/service_influxdb"]


####################################
## Site-specific variables
####################################
site_variables:
  template_id: ID of the InfluxDB VM template to use in your OpenNebula environment
  image_id: ID of the InfluxDB VM image to use in your OpenNebula environment


####################################
## Input variables
####################################
input:
  one_influxdb_networks:
    description: Ordered list of Virtual Network names the VM will be part of. The first Virtual Network is used by Jenkins to reach and configure the VM, so most cases will be fine using always the tn_vxlan
    type: "list[tn_vxlan or vnet]"
    default_value: ["tn_vxlan"]
    required_when: false
  one_influxdb_user:
    description: Username used to login into the InfluxDB
    type: str
    default_value: "admin"
    required_when: false
  one_influxdb_password:
    description: Password used to login into the InfluxDB
    type: str
    default_value: "adminadmin"
    required_when: true
  one_influxdb_org:
    description: Organization name
    type: str
    default_value: "dummyorg"
    required_when: false
  one_influxdb_bucket:
    description: Bucket name
    type: str
    default_value: "dummybucket"
    required_when: false
  one_influxdb_token:
    description: Token used to login into the InfluxDB
    type: str
    default_value: "dummydummytoken"
    required_when: true


####################################
## Generated terraform outputs
####################################
terraform_outputs:
  __entity_name__id: "VM ID in OpenNebula. Generated from Terraform Manifest"
  __entity_name__ips: "Dictionary of VM IP addresses: {<VNet ID in OpenNebula>: <IP address>}. Generated from Terraform Manifest"
