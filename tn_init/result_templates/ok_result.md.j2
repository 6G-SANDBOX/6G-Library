# {{ tn_id }}-tn_init

The Trial Network `{{ tn_id }}` has been successfully created.
The component `{{ tn_id }}-tn_vxlan` is the main subnetwork of the Trial Network, reachable with the bastion's VPN
Component `{{ tn_id }}-tn_bastion` is a crutial VM of the Trial Network, which serves as its router, firewall and VPN endpoing.

## Important information

#### {{ tn_id }}-tn_vxlan

- **OpenNebula VNet ID**: `{{ tn_vxlan_id }}`
- **VXLAN first IP**: `{{ one_vxlan_first_ip }}`
- **VXLAN address size**: `{{ one_vxlan_address_size }}`
- **VXLAN netmask**: `{{ one_vxlan_netmask }}`
- **VXLAN gateway**: `{{ one_vxlan_first_ip }}`
- **VXLAN DNS**: `{{ one_vxlan_first_ip }}`
- **VXLAN MTU**: `{{ one_vxlan_mtu }}`
- **VXLAN guest MTU**: `{{ one_vxlan_guest_mtu }}`

#### {{ tn_id }}-tn_bastion

- **OpenNebula VM ID**: `{{ tn_bastion_id }}`
- **VM network interfaces**:
  ```json
  {{ tn_bastion_ips }}
  ```

#### Wireguard VPN client config
Add this configuration file to your local wireguard client tool.
```
{{ hostvars['tn_bastion']['client_tunnel']['content'] | b64decode }}
```

#### SSH keypair
Use the private key to access Trial Network VM's with user *tnuser*

**Public key**:
```
{{ hostvars['tn_bastion']['tn_ssh_public_key']['content'] | b64decode }}
```
**Private key**:
```
{{ hostvars['tn_bastion']['tn_ssh_private_key']['content'] | b64decode }}
```

