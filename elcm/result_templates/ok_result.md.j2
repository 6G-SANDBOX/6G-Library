# {{ tn_id }}-{{ entity_name }}

The component `{{ tn_id }}-{{ entity_name }}` has been succesfully created.

## Important information:

- **OpenNebula VM ID**: `{{ id }}`
- **VM memory**: `{{ one_elcm_memory}} MiB`
- **VM VCPUs**: `{{ one_elcm_cpu }}`
- **VM available storage**: `7 GiB`
- **VM network interfaces**:
  ```json
  {{ ips }}
  ```

## ELCM BACKEND

The backend dashboard is available on port 5000.

[http://{{ domain_name }}:5000](http://{{ domain_name }}:5000)

## ELCM FRONTEND

The frontend dashboard is available on port 5001.

[http://{{ domain_name }}:5000](http://{{ domain_name }}:5001)

## InfluxDB v2

InfluxDB v2 is available on port 8086.

[http://{{ domain_name }}:8086](http://{{ domain_name }}:8086)

Information to access to InfluxDB v2 dashboard:

- **Username**: `{{ one_elcm_influxdb_user }}`
- **Password**: `{{ one_elcm_influxdb_password }}`
- **Organization**: `{{ one_elcm_influxdb_org }}`
- **Bucket**: `{{ one_elcm_influxdb_bucket }}`

## Grafana

Grafana is available on port 3000.

[http://{{ domain_name }}:3000](http://{{ domain_name }}:3000)

Information to access to Grafana dashboard:

- **Username**: `admin`
- **Password**: `{{ one_elcm_grafana_password }}`
