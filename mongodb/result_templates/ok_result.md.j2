# {{ tn_id }}-{{ entity_name }}

The component `{{ tn_id }}-{{ entity_name }}` has been succesfully created.

## Important information:

- **OpenNebula VM ID**: `{{ id }}`
- **VM memory**: `{{ one_mongodb_memory }} MiB`
- **VM VCPUs**: `{{ one_mongodb_cpu }}`
- **VM available storage**: `7 GiB`
- **VM network interfaces**:
  ```json
  {{ ips }}
  ```

## MongoDB v{{ one_mongodb_version }} information:

- **Username**: `{{ one_mongodb_user }}`
- **Password**: `{{ one_mongodb_password }}`
- **Database**: `{{ one_mongodb_database }}`

## Mongo express information:

[http://{{ domain_name }}:8081](http://{{ domain_name }}:8081)

- **Username**: `{{ one_mongo_express_user }}`
- **Password**: `{{ one_mongo_express_password }}`
