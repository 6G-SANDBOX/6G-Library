---
- name: "Set variables for the terraform manifest according to the selected appliance"
  ansible.builtin.set_fact:
    one_memory: >
      {{
          one_srs_open5gs_memory
       }}
    template_id: >-
      {{
      
          site_available_components.srs_open5gs.template_id | default(site_available_components.srs_open5gs.template_id)
       }}
    image_id: >-
      {{ 
          site_available_components.srs_open5gs.image_id | default(site_available_components.srs_open5gs.image_id)
       }}


- name: Overwrite again with explicit values from the input file
  ansible.builtin.include_vars:
    file: "{{ workspace }}/{{ component_type }}/variables/input_file.yaml"
