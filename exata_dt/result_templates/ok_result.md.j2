# {{ tn_id }}-{{ entity_name}}

The component `{{ tn_id }}-{{ entity_name }}` has been succesfully created.

It consists of an EXata VM, which allows Experimenters to include EXata DTs as part of a Trial Network, in turn enabling the programmatic retrieval of insights and predictions about the twinned live network.

## Important information:

- **OpenNebula VM ID**: `{{ id }}`
- **VM memory**: `{{ one_exata_dt_memory}} MiB`
- **VM VCPUs**: `{{ one_exata_dt_cpu }}`
- **VM internal storage**: `{{ one_exata_dt_disk}} MiB`
- **VM IP addresses**:
{% for ip_list in ips.values() %}
{% for ip in ip_list %}
  - `{{ ip }}`
{% endfor %}
{% endfor %}